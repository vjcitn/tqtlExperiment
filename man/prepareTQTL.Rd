% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runTQTL.R
\name{prepareTQTL}
\alias{prepareTQTL}
\title{Prepare input files and return the tensorQTL CLI command.
Writes the phenotype BED file and (optionally) the covariate file to a
user-specified directory, then returns the complete \verb{python -m tensorqtl}
command string.  The user runs this command in a terminal where their
Python/conda environment is properly configured, then calls \code{\link[=readTQTL]{readTQTL()}}
to load the results back into R.}
\usage{
prepareTQTL(
  x,
  outDir,
  mode = c("cis_nominal", "cis", "cis_independent", "trans"),
  assayName = NULL,
  mafThreshold = 0.05,
  window = 1000000L,
  permutations = 1000L,
  python = .detect_python(),
  ...
)
}
\arguments{
\item{x}{A \link{tQTLExperiment}.}

\item{outDir}{Path to an existing directory that will hold both the
intermediate input files and the tensorQTL output.  \strong{Must be specified
by the caller} - no default is provided.}

\item{mode}{One of \code{"cis_nominal"}, \code{"cis"}, \code{"cis_independent"},
\code{"trans"}. Passed to \code{--mode}.}

\item{assayName}{Name of the assay to use as the phenotype matrix.
Defaults to the first assay.}

\item{mafThreshold}{MAF threshold. Defaults to \code{0.05}.}

\item{window}{Cis-window in base pairs. Defaults to \code{1000000}.}

\item{permutations}{Permutations for \code{"cis"} mode. Defaults to \code{1000}.}

\item{python}{Path to the Python executable to embed in the returned
command string. Defaults to auto-detection: uses the Python inside the
active conda environment (\code{CONDA_PREFIX}) or virtualenv
(\code{VIRTUAL_ENV}) if set, otherwise the first \code{python3} on
\code{PATH}.}

\item{...}{Additional \verb{--key value} flags passed to tensorQTL verbatim.}
}
\value{
A character string containing the complete shell command to run.
The string is also printed via \code{\link[=message]{message()}} for easy copy-paste.
}
\description{
Prepare input files and return the tensorQTL CLI command.
Writes the phenotype BED file and (optionally) the covariate file to a
user-specified directory, then returns the complete \verb{python -m tensorqtl}
command string.  The user runs this command in a terminal where their
Python/conda environment is properly configured, then calls \code{\link[=readTQTL]{readTQTL()}}
to load the results back into R.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed")
)
cmd <- prepareTQTL(tqe, outDir = tempdir(), mode = "cis_nominal")
cat(cmd, "\n")

}
\seealso{
\code{\link[=readTQTL]{readTQTL()}} to load results after the command has been run.
}
