% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addGeneSymbols.R
\name{addGeneSymbols}
\alias{addGeneSymbols}
\title{Add gene symbols to rowRanges}
\usage{
addGeneSymbols(x, ensdb = NULL)
}
\arguments{
\item{x}{A \link{tQTLExperiment} (or any
\link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment} with Ensembl IDs as
\code{rownames}).}

\item{ensdb}{An \link[ensembldb:EnsDb]{ensembldb::EnsDb} object.  Defaults to
\code{EnsDb.Hsapiens.v79::EnsDb.Hsapiens.v79} if that package is installed.}
}
\value{
\code{x} with an additional \code{gene_name} metadata column in
\code{rowRanges(x)}.
}
\description{
Looks up HGNC gene symbols for Ensembl gene IDs in \code{rownames(x)} using an
\link[ensembldb:EnsDb]{ensembldb::EnsDb} annotation object and adds them as a \code{gene_name} column
in \code{mcols(rowRanges(x))}.  Version suffixes (e.g. \code{.3} in
\code{ENSG00000100181.3}) are stripped before lookup.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    genome      = "hg38"
)
if (requireNamespace("EnsDb.Hsapiens.v79", quietly = TRUE))
    tqe <- addGeneSymbols(tqe)

}
