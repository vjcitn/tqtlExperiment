% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_mage_chr17_plink.R
\name{cache_mage_chr17_plink}
\alias{cache_mage_chr17_plink}
\title{Cache MAGE chr17 PLINK files from GitHub releases}
\usage{
cache_mage_chr17_plink(
  release_url = paste0("https://github.com/vjcitn/CSHLvc2026/",
    "releases/download/v0.1.0-data"),
  bfc = BiocFileCache::BiocFileCache(),
  verbose = TRUE,
  validate_bed = TRUE
)
}
\arguments{
\item{release_url}{Base URL of the GitHub release assets.  Defaults to
the \code{v0.1.0-data} release of \code{vjcitn/CSHLvc2026}.}

\item{bfc}{A \link[BiocFileCache:BiocFileCache]{BiocFileCache::BiocFileCache} object.  Defaults to the
user-level cache.}

\item{verbose}{Logical.  Emit progress messages?  Default \code{TRUE}.}

\item{validate_bed}{Logical.  Check \code{.bed} magic bytes after download?
Default \code{TRUE}.}
}
\value{
A named list with elements \code{fam}, \code{bim}, and \code{bed}, each a
character scalar giving the local path to the cached file.  All three
share the same directory and stem, so
\code{tools::file_path_sans_ext(result[["bed"]])} gives the PLINK prefix
for use with \code{\link[=tQTLExperimentFromRSE]{tQTLExperimentFromRSE()}}.
}
\description{
Downloads the three PLINK files (\code{CCDG_mage_chr17.fam}, \code{.bim}, \code{.bed})
from the \code{vjcitn/CSHLvc2026} GitHub release into a \code{BiocFileCache}
subdirectory.  Files are only re-downloaded when absent from the cache.
No authentication is required.
}
\examples{
\dontrun{
plink <- cache_mage_chr17_plink()
plpre <- tools::file_path_sans_ext(plink[["bed"]])
}
}
