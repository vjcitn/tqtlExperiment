<div id="main" class="col-md-9" role="main">

# Package index

<div class="section level2">

## All functions

</div>

<div class="section level2">

-   `addGeneSymbols()` : Add gene symbols to rowRanges
-   `cache_mage_chr17_plink()` : Cache MAGE chr17 PLINK files from
    GitHub releases
-   `findTQTL()` : Find a Python installation with tensorQTL
-   `plotGenotypeEffect()` : Plot phenotype values grouped by SNP
    genotype
-   `prepareTQTL()` : Prepare input files and return the tensorQTL CLI
    command
-   `readTQTL()` : Read tensorQTL results into R
-   `show(<tQTLExperiment>)` : Show a tQTLExperiment
-   `` `[`(<tQTLExperiment>,<ANY>,<ANY>,<ANY>) `` : Subset a
    tQTLExperiment
-   `tQTLExperiment-class` : tQTLExperiment: a SummarizedExperiment for
    tensorQTL eQTL mapping
-   `tQTLExperiment()` : Construct a tQTLExperiment from input files
-   `tQTLExperimentFromRSE()` : Construct a tQTLExperiment from a
    RangedSummarizedExperiment
-   `tensorqtlVersion()` : Report the tensorQTL version from a Python
    installation
-   `tqtlGeno()` `` `tqtlGeno<-`() `` : Access the lazy genotype matrix
-   `tqtlPlinkPrefix()` `` `tqtlPlinkPrefix<-`() `` : Access the PLINK
    file prefix
-   `tqtlVariantRanges()` `` `tqtlVariantRanges<-`() `` : Access variant
    genomic ranges

</div>

</div>
