% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtlRegressionStatsFast.R
\name{qtlRegressionStatsFast}
\alias{qtlRegressionStatsFast}
\title{Fast linear regression statistics for a collection of SNP-gene pairs}
\usage{
qtlRegressionStatsFast(
  tqe,
  pairs,
  assayName = NULL,
  wideFormat = TRUE,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{tqe}{A \link{tQTLExperiment} with genotype data.}

\item{pairs}{A data frame with columns \code{phenotype_id} and
\code{variant_id} identifying the SNP-gene pairs to analyse.}

\item{assayName}{Name of the assay to use. Defaults to the first assay.}

\item{wideFormat}{Logical. If \code{TRUE} (default), pivot to one row per
pair with two columns per model term (\code{estimate.<term>} and
\code{std.error.<term>}) suitable for multivariate analysis. If
\code{FALSE}, return the long tidy form with one row per pair x term
including \code{statistic} and \code{p.value}.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} object controlling
parallelisation. Defaults to \code{\link[BiocParallel:SerialParam]{BiocParallel::SerialParam()}} (single
core). Use e.g. \code{\link[BiocParallel:MulticoreParam]{BiocParallel::MulticoreParam()}} for multicore.}
}
\value{
Same structure as \code{\link[=qtlRegressionStats]{qtlRegressionStats()}}: wide format (default)
with two columns per model term, or long format with one row per pair x
term.
}
\description{
Optimised version of \code{\link[=qtlRegressionStats]{qtlRegressionStats()}} that uses QR rank-1 updates
from the \pkg{fastQR} package.  For each phenotype group the covariate
design matrix is factorised once via \code{qr_lm_init()} (O(np\eqn{^2}));
each SNP within the group then adds its genotype column to the existing QR
via \code{qr_lm_update()} (O(np)) rather than re-factorising from scratch.
Phenotype groups are processed in parallel via \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}}.
}
\details{
\pkg{fastQR} is not on CRAN or Bioconductor; install it from the local
source or GitHub before calling this function.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    covFile     = file.path(exdir, "cov-n100-tqtl.tsv"),
    genome      = "hg38"
)
pairs <- data.frame(
    phenotype_id = rownames(tqe)[1:3],
    variant_id   = S4Vectors::mcols(tqtlVariantRanges(tqe))[["snp_id"]][1:3]
)
if (requireNamespace("fastQR", quietly = TRUE))
    qtlRegressionStatsFast(tqe, pairs)
}
