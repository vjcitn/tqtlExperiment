% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runTQTL.R
\name{runTQTL}
\alias{runTQTL}
\alias{runTQTL,tQTLExperiment-method}
\title{Run the tensorQTL eQTL mapping CLI}
\usage{
runTQTL(
  x,
  python = NULL,
  mode = c("cis_nominal", "cis", "cis_independent", "trans"),
  assayName = NULL,
  mafThreshold = 0.05,
  window = 1000000L,
  permutations = 1000L,
  debug = FALSE,
  ...
)

\S4method{runTQTL}{tQTLExperiment}(
  x,
  python = NULL,
  mode = c("cis_nominal", "cis", "cis_independent", "trans"),
  assayName = NULL,
  mafThreshold = 0.05,
  window = 1000000L,
  permutations = 1000L,
  debug = FALSE,
  ...
)
}
\arguments{
\item{x}{A \link{tQTLExperiment}.}

\item{python}{Path to the Python executable with tensorqtl installed.
Defaults to \code{\link[=findTQTL]{findTQTL()}}.}

\item{mode}{One of \code{"cis"}, \code{"cis_nominal"}, \code{"cis_independent"},
\code{"trans"}. Passed to \code{--mode}. Defaults to \code{"cis_nominal"}.}

\item{assayName}{Name of the assay to use as phenotype. Defaults to
first assay.}

\item{mafThreshold}{MAF threshold for variant filtering. Passed to
\code{--maf_threshold}. Defaults to \code{0.05}.}

\item{window}{Cis-window size in base pairs. Defaults to \code{1000000}.}

\item{permutations}{Number of permutations for \code{"cis"} mode. Ignored for
\code{"cis_nominal"}.}

\item{debug}{Logical; if \code{TRUE}, the temporary directory is kept after
the run and its path is printed along with the first lines of each
input file.}

\item{...}{Additional arguments passed to the tensorQTL CLI as
\verb{--key value} pairs.}
}
\value{
A named list of \link[GenomicRanges:GRanges]{GenomicRanges::GRanges}:
\describe{
\item{\code{pairs}}{For \code{cis_nominal}: all tested feature–variant pairs.}
\item{\code{hits}}{For \code{cis}: one range per feature with permutation
p-value.}
}
The exact contents depend on mode; raw output files are preserved when
\code{debug = TRUE}.
}
\description{
Writes temporary input files from a \link{tQTLExperiment}, invokes
\verb{python -m tensorqtl}, reads the results, and returns them as a named
list of \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} objects.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed")
)
tryCatch({
    res <- runTQTL(tqe, mode = "cis_nominal")
    res$pairs
}, error = function(e) invisible(NULL))

}
