% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cis_qtl_browser.R
\name{run_cis_qtl_browser}
\alias{run_cis_qtl_browser}
\title{Interactive cis-QTL browser}
\usage{
run_cis_qtl_browser(
  tqe,
  cis_gr,
  gene_col = "gene_symbol",
  snp_col = "variant_id",
  phenotype_col = "phenotype_id",
  pval_col = "pval_nominal",
  pval_thresh = NULL
)
}
\arguments{
\item{tqe}{A \link{tQTLExperiment} with genotype data.}

\item{cis_gr}{A \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} object containing cis-QTL results
(e.g., from \code{\link[=readTQTL]{readTQTL()}}).}

\item{gene_col}{Name of the metadata column in \code{cis_gr} containing gene
symbols. Default: \code{"gene_symbol"}.}

\item{snp_col}{Name of the metadata column in \code{cis_gr} containing SNP IDs.
Default: \code{"variant_id"}.}

\item{phenotype_col}{Name of the metadata column in \code{cis_gr} containing
phenotype/gene IDs. Default: \code{"phenotype_id"}.}

\item{pval_col}{Name of the metadata column in \code{cis_gr} containing p-values.
Default: \code{"pval_nominal"}.}

\item{pval_thresh}{Optional numeric threshold for filtering associations by
p-value. If \code{NULL} (default), all associations are included.}
}
\value{
A Shiny application object (invisibly). Call this function in an
interactive R session to launch the browser.
}
\description{
Launches a Shiny application for exploring cis-QTL associations.
Users can select genes, view associated SNPs, and visualize the genotype
effect on phenotypes using \code{\link[=plotGenotypeEffect]{plotGenotypeEffect()}}.
}
\details{
The browser provides two tabs:
\itemize{
\item \strong{Genotype effect}: Beeswarm plot of phenotype values by SNP genotype,
generated by \code{\link[=plotGenotypeEffect]{plotGenotypeEffect()}}.
\item \strong{cis associations}: Filterable data table of all associations for the
selected gene.
}

Navigation is via dropdown menus: select a gene symbol (searchable), then
a cis-SNP for that gene, then click "Plot" to visualize.
}
\examples{
if (interactive()) {
  # Build example tQTLExperiment
  exdir <- system.file("extdata", package = "tQTLExperiment")
  tqe <- tQTLExperiment(
      plinkPrefix = file.path(exdir, "chr22-n100"),
      phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
      genome      = "hg38"
  )

  # Load demo results
  demodir <- system.file("demodir", package = "tQTLExperiment")
  res <- readTQTL(demodir, mode = "cis_nominal")

  # Launch browser
  app <- run_cis_qtl_browser(tqe, res$pairs, pval_thresh = 0.05)
}
}
