% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tQTLExperiment-class.R
\docType{class}
\name{tQTLExperiment-class}
\alias{tQTLExperiment-class}
\title{tQTLExperiment: a SummarizedExperiment for tensorQTL eQTL mapping}
\description{
\code{tQTLExperiment} extends \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment}
to hold all data needed for the tensorQTL eQTL mapping tool:
}
\details{
\itemize{
\item \strong{assays}: phenotype matrix (features × samples)
\item \strong{rowRanges}: genomic coordinates for each phenotype feature (gene)
\item \strong{colData}: per-sample metadata including covariates
\item \strong{geno}: lazy \link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix} over the PLINK \code{.bed} file
(samples × variants); data are not loaded until subscripted
\item \strong{variantRanges}: \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} for each variant, with
metadata columns \code{snp_id}, \code{ref}, \code{alt}
\item \strong{plinkPrefix}: path prefix for the PLINK file set
}

tensorQTL is invoked via its CLI (\verb{python -m tensorqtl}) and supports
GPU acceleration through CUDA or Apple MPS (Metal Performance Shaders)
on Apple Silicon.
}
\section{Slots}{

\describe{
\item{\code{geno}}{A \link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix} (samples × variants).}

\item{\code{variantRanges}}{A \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} of length equal to
\code{ncol(geno)}.}

\item{\code{plinkPrefix}}{A length-one character string giving the PLINK file
prefix.}
}}

\seealso{
\code{\link[=tQTLExperiment]{tQTLExperiment()}} for the constructor, \code{\link[=runTQTL]{runTQTL()}} to invoke
tensorQTL, \code{\link[=findTQTL]{findTQTL()}} to locate the Python installation.
}
