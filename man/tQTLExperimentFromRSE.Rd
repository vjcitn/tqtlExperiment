% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tQTLExperiment-constructor.R
\name{tQTLExperimentFromRSE}
\alias{tQTLExperimentFromRSE}
\title{Construct a tQTLExperiment from a RangedSummarizedExperiment}
\usage{
tQTLExperimentFromRSE(
  se,
  plinkPrefix,
  covariateMatrix = NULL,
  assayName = NULL,
  featureIdColumn = NULL,
  genome = NA_character_
)
}
\arguments{
\item{se}{A \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment}.}

\item{plinkPrefix}{Path prefix for the PLINK file set.}

\item{covariateMatrix}{A numeric matrix (rows = samples, cols =
covariates) built e.g. with \code{\link[stats:model.matrix]{stats::model.matrix()}} on
\code{as.data.frame(colData(se))}. See also the
\href{https://bioconductor.org/packages/ExploreModelMatrix}{ExploreModelMatrix}
package. Rename \code{(Intercept)} to \code{int} if including an intercept.}

\item{assayName}{Name of the assay in \code{se} to use. Defaults to first.}

\item{featureIdColumn}{Optional mcols column name to use as
\code{phenotype_id}; defaults to \code{rownames(se)}.}

\item{genome}{Genome build string (e.g. \code{"hg38"}) assigned to the
\code{seqinfo} of \code{rowRanges}. Defaults to \code{NA} (unspecified).}
}
\value{
A \link{tQTLExperiment} object.
}
\description{
Converts an existing \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment}
into a \link{tQTLExperiment} by attaching PLINK genotype data.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe_ref <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed")
)
se <- as(tqe_ref, "RangedSummarizedExperiment")
cd <- as.data.frame(colData(tqe_ref))
mm <- model.matrix(~ 1, data = cd)
colnames(mm)[1] <- "int"
tqe2 <- tQTLExperimentFromRSE(se,
    plinkPrefix     = file.path(exdir, "chr22-n100"),
    covariateMatrix = mm)
tqe2

}
