% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtlRegressionStats.R
\name{qtlRegressionStats}
\alias{qtlRegressionStats}
\title{Linear regression statistics for a collection of SNP-gene pairs}
\usage{
qtlRegressionStats(
  tqe,
  pairs,
  assayName = NULL,
  t_only = TRUE,
  wideFormat = TRUE,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{tqe}{A \link{tQTLExperiment} with genotype data.}

\item{pairs}{A data frame with columns \code{phenotype_id} and
\code{variant_id} identifying the SNP-gene pairs to analyse.}

\item{assayName}{Name of the assay to use. Defaults to the first assay.}

\item{t_only}{Logical. If \code{TRUE} (default), return only the
t-statistic (estimate / std.error) for each model term. In wide format
columns are named \code{<term>.t}. If \code{FALSE}, return estimates and
standard errors (and additionally statistic and p.value in long format).}

\item{wideFormat}{Logical. If \code{TRUE} (default), pivot to one row per
pair. With \code{t_only = TRUE}: one column per term named
\code{<term>.t}. With \code{t_only = FALSE}: two columns per term,
\code{estimate.<term>} and \code{std.error.<term>}. If \code{FALSE},
return the long tidy form with one row per pair x term.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} object controlling
parallelisation. Defaults to \code{\link[BiocParallel:SerialParam]{BiocParallel::SerialParam()}} (single
core). Use e.g. \code{\link[BiocParallel:MulticoreParam]{BiocParallel::MulticoreParam()}} for multicore.}
}
\value{
A data frame. See \code{t_only} and \code{wideFormat} for the
shape of the output.
}
\description{
For each (phenotype_id, variant_id) pair, fits a linear model of phenotype
on genotype plus all covariates stored in the \link{tQTLExperiment} and returns
a tidy data frame of coefficients, standard errors, t-statistics, and
p-values for every model term.  Pairs are grouped by phenotype and processed
in parallel via \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}}.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    covFile     = file.path(exdir, "cov-n100-tqtl.tsv"),
    genome      = "hg38"
)
pairs <- data.frame(
    phenotype_id = rownames(tqe)[1],
    variant_id   = S4Vectors::mcols(tqtlVariantRanges(tqe))[["snp_id"]][1]
)
qtlRegressionStats(tqe, pairs)
qtlRegressionStats(tqe, pairs, t_only = FALSE, wideFormat = FALSE)
}
