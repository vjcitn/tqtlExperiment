% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tQTLExperiment-constructor.R
\name{tQTLExperiment}
\alias{tQTLExperiment}
\title{Construct a tQTLExperiment from input files}
\usage{
tQTLExperiment(plinkPrefix, phenoFile, covFile = NULL, assayName = "pheno")
}
\arguments{
\item{plinkPrefix}{Path prefix for the PLINK file set (\code{.bed}, \code{.bim},
\code{.fam}).}

\item{phenoFile}{Path to the phenotype BED file (tab-separated, columns
\verb{#chr}, \code{start}, \code{end}, \code{phenotype_id}, then one column per sample;
0-based half-open coordinates).}

\item{covFile}{Path to the covariate file. tensorQTL expects covariates
as rows with the first column being the covariate name and remaining
columns being sample values (covariates × samples). If \code{NULL}, no
covariate file is used.}

\item{assayName}{Name to assign the phenotype matrix in \code{assays()}.
Defaults to \code{"pheno"}.}
}
\value{
A \link{tQTLExperiment} object.
}
\description{
Reads phenotype, covariate, and variant metadata files into a
\link{tQTLExperiment} object. Genotype data is represented lazily via
\link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix}.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    covFile     = file.path(exdir, "cov-n100-tqtl.tsv")
)
tqe

}
