% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runTQTL.R
\name{readTQTL}
\alias{readTQTL}
\title{Read tensorQTL results into R}
\usage{
readTQTL(
  outDir,
  mode = c("cis_nominal", "cis", "cis_independent", "trans"),
  x = NULL
)
}
\arguments{
\item{outDir}{The same directory passed to \code{\link[=prepareTQTL]{prepareTQTL()}}.}

\item{mode}{The mode used for the tensorQTL run.}

\item{x}{The \link{tQTLExperiment} used to generate the input files, used to
attach feature coordinates to region-level results.}
}
\value{
A named list of \link[GenomicRanges:GRanges]{GenomicRanges::GRanges}:
\describe{
\item{\code{pairs}}{(\code{cis_nominal}) Feature-variant pairs with association
statistics.}
\item{\code{hits}}{(\code{cis}) One range per feature with permutation p-value.}
\item{\code{output_files}}{(other modes) Paths to the raw output files.}
}
}
\description{
Reads the output files written by a tensorQTL run into
\link[GenomicRanges:GRanges]{GenomicRanges::GRanges} objects, matched to the features in the
originating \link{tQTLExperiment}.
}
\examples{
exdir <- system.file("extdata", package = "tQTLExperiment")
tqe <- tQTLExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed")
)
od <- tempdir()
cmd <- prepareTQTL(tqe, outDir = od, mode = "cis_nominal")
# (user runs cmd in terminal)
# res <- readTQTL(od, mode = "cis_nominal", x = tqe)

}
\seealso{
\code{\link[=prepareTQTL]{prepareTQTL()}} to write input files and obtain the CLI command.
}
